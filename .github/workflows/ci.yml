name: QVPIC CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      TORCH_CUDA_ARCH_LIST: ""   # prevents any accidental CUDA compilation on CI runners

    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: "pip"

      - name: Install dependencies (editable + dev)
        run: |
          python -m pip install --upgrade pip
          
          # 🔥 Force CPU-only Torch (GitHub runners have no GPU)
          pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
          
          # Install the rest of the project
          pip install -e ".[dev]"

      - name: Run tests with coverage
        run: |
          pytest -v --maxfail=5 --durations=10 --cov=src --cov-report=xml

      - name: Lint with ruff
        run: |
          ruff check src/ scripts/ --output-format=github

      - name: Check formatting (black)
        run: |
          black --check --diff src/ scripts/

      - name: Check import sorting (isort)
        run: |
          isort --check-only --diff src/ scripts/

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          files: ./coverage.xml
          fail_ci_if_error: false
