# =============================================
# QVPIC v10.2 — Quaternion Vortex Persistent Identity Conduit
# Requirements file — production-ready + reproducible
# =============================================

# ── Core Runtime (always required) ──
torch>=2.4.0                    # PyTorch 2.4+ recommended (CUDA 12.x support)
numpy>=1.21.0
scipy>=1.10.0                   # Required for Laguerre-Gaussian modes (genlaguerre)
matplotlib>=3.5.0
omegaconf>=2.3.0                # Config loading (default.yaml)
sentence-transformers>=2.2.0    # Fact embedding in qvpic_test.py
scikit-learn>=1.0.0             # FastICA used in patent encode/decode examples
tqdm>=4.60.0                    # Progress bars in tests and simulations

# ── UI / Agent Demo ──
gradio>=4.0.0                   # Gradio interface in agent_demo.py

# ── Testing ──
pytest>=7.0.0

# ── Optional: Local LLM support (used in agent_demo.py) ──
llama-cpp-python>=0.2.0

# ── Optional: Development / Formatting tools (uncomment if you want) ──
# black>=24.0.0
# isort>=5.0.0
# ruff>=0.0.0

# =============================================
# Installation notes:
#   pip install -r requirements.txt
#
# For CUDA support (recommended):
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#
# After install, run:
#   python scripts/qvpic_test.py --strong-train --no-viz
# =============================================