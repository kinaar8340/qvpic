# Binary model checkpoints and weights - never diff or merge as text
*.pt          binary
*.pth         binary
*.bin         binary
*.gguf        binary
*.safetensors binary

# Entire checkpoints directory (future-proof)
checkpoints/** binary

# Large model files
models/**     binary
